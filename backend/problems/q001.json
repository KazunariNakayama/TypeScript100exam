{
  "id": "q001",
  "title": "配列の合計を求めよ（sum）",
  "description": "number[] を受け取り、要素の合計を返す関数 sum を実装してください。空配列なら 0 を返すこと。",
  "starterCode": "function sum(arr: number[]): number {\n  // TODO: reduce または for-of で合計\n  return 0;\n}",
  "testCode": "console.assert(sum([1, 2, 3]) === 6, 'sum([1,2,3]) !== 6');\nconsole.assert(sum([]) === 0, 'sum([]) !== 0');\nconsole.assert(sum([-1, 0, 1]) === 0, 'sum([-1,0,1]) !== 0');",
  "hint": "reduce の初期値を 0 にすることで空配列でも安全です。",
  "answer": "function sum(arr: number[]): number {\n  return arr.reduce((a, v) => a + v, 0);\n}",
  "explanation": "初期値 0 を渡せば、空配列でも例外にならずに 0 が返せます。",
  "alternativeSolutions": [
    "function sum(arr: number[]): number { let t=0; for (const v of arr) t+=v; return t; }"
  ]
}
